<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mainContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:background="@drawable/story_selection_bg_landscape"
    android:padding="24dp">

    <!-- Левая панель - заголовок и описание -->
    <LinearLayout
        android:id="@+id/leftPanel"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:id="@+id/titleText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Choose Your Adventure"
            android:textSize="28sp"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <TextView
            android:id="@+id/storyDescription"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="Select a story from the list to begin your journey..."
            android:textSize="16sp"
            android:textColor="@color/white"
            android:gravity="top"
            android:lineSpacingExtra="4dp"
            android:padding="16dp"
            android:background="@drawable/rounded_panel" />

        <Button
            android:id="@+id/continueButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Begin Adventure"
            android:textSize="18sp"
            android:layout_marginTop="16dp"
            android:enabled="false"
            style="@style/PrimaryButton" />

    </LinearLayout>

    <!-- Правая панель - список историй -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/storiesRecyclerView"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1.5"
        android:layout_marginStart="24dp"
        android:padding="16dp"
        android:background="@drawable/rounded_panel" />

</LinearLayout>